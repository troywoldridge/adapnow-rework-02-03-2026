import { describe, it } from "vitest";

/**
 * Deprecated duplicate test file.
 * Stage 2 authz coverage lives in:
 *   src/lib/__tests__/authzPolicy.test.ts
 *
 * Keeping this file as a stub so CI doesn't fail due to a missing file reference.
 */
describe("authzPolicy.stage2 (stub)", () => {
  it("skipped: use authzPolicy.test.ts", () => {
    // no-op
  });
});
