// src/components/search/ImageSafe.tsx
// Alias-free re-export for search components.
// Keep this file server-safe (no "use client") to avoid forcing a client boundary.

export { default } from "../ImageSafe";
export * from "../ImageSafe";
